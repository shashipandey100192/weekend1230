import React from 'react'
import {Button} from '@mui/material';

function Lazypage() {
  return (
    <div className='container-fluid'>
        <div className='row'>
            <div className='col-12'>
                <p>Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. Lazy loading in React.js is a performance optimization technique that defers the loading of non-essential resources, such as components or images, until they are actually needed by the user. This contrasts with traditional loading, where all resources are loaded upfront when the application initially renders. 
How it works:
Code Splitting:
Lazy loading relies on code splitting, a feature supported by bundlers like Webpack. This process divides the application's code into smaller, independent chunks.
Dynamic Imports:
Instead of importing components directly at the top of a file, dynamic import() statements are used. These imports are asynchronous and only fetch the necessary code chunk when the component is about to be rendered. </p>
            <p>
  While using the property will wrap the element to prevent the
  text inside from extending beyond its parent.
</p>

<div class="flex">
  <div class="w-14 flex-none border-4 border-indigo-500">01</div>
  <div class="w-64 flex-1 ...">02</div>
  <div class="w-32 flex-1 ...">03</div>
</div>

<Button variant="text">Text</Button>
<Button variant="contained">Contained</Button>
<Button variant="outlined">Outlined</Button>

            
            </div>
        </div>
    </div>
  )
}

export default Lazypage